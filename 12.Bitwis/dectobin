#include<stdio.h>
void main()
{
  int num,bin,rem=0;
  printf("enter an integer number..");
  scanf("%d",&num);
  while(num>0)
  {
   rem=num%2;
   bin=bin+10*rem;
   num=num/2;
  }
  printf("binary number...%d",bin);
}
